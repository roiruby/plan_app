    <div class="nested-fields">
      
      <div class="form-group">
        <%= f.label :schedule_title, 'スケジュールタイトルを入力' %>
        <%= f.text_field :schedule_title, placeholder: 'スケジュールの見出しを入力　(50文字以内)', class: 'form-control border-warning' %>
      </div>
      
      <div class="form-group">
        <%= f.label :start_time, '開始時刻　' %>
        <%= f.text_field :start_time, placeholder: '例) 09:00', class: 'form-control border-warning' %>
      </div>
      
      <div class="form-group">
        <%= f.label :end_time, '終了時刻　' %>
        <%= f.text_field :end_time, placeholder: '例) 13:00', class: 'form-control border-warning' %>
      </div>
      
      
      <div class="form-group">
        <%= f.label :sub_title, 'サブタイトル' %>
        <%= f.text_field :sub_title, placeholder: '画像のタイトルなど (50文字以内)', class: 'form-control border-warning' %>
      </div>
      
      
      <div class="plan-image row row-0">

          <div class="col-6 fileup pr-1">
          <% if f.object.image1? %>
            <%= image_tag f.object.image1.url, id: :img1_prev %>
            <p><%= f.check_box :remove_image1 %>画像を削除する</p>
          <% else %>
            <%= image_tag "default_plan1.jpg", id: :img1_prev %>
          <% end %>
            <label>
              <div class="xsmall">
              ファイルを選択
              </div>
              <div class="xsmall upload"><%= f.file_field :image1, class: :schedule1_img %></div>
            </label>
          </div>

        
        <!--画像即時にプレビューjavascript-->
        <script type="text/javascript">
        $(function() {
         function readURL(input) {
         if (input.files && input.files[0]) {
         var reader = new FileReader();
         reader.onload = function (e) {
           $(input).parents('.plan-image').find('#img1_prev').attr('src', e.target.result);
         }
         reader.readAsDataURL(input.files[0]);
         }
         }
         $(document).on('change', '.schedule1_img', function(){
         readURL(this);
         });
        });
        </script>
        
        
        <div class="col-6 fileup pl-1">
          <% if f.object.image2? %>
            <%= image_tag f.object.image2.url, id: :img2_prev %>
            <p><%= f.check_box :remove_image2 %>画像を削除する</p>
          <% else %>
            <%= image_tag "default_plan2.jpg", id: :img2_prev %>
          <% end %>
            <label>
              <div class="xsmall">
              ファイルを選択
              </div>
              <div class="xsmall upload"><%= f.file_field :image2, class: :schedule2_img %></div>
            </label>
        </div>
        
        <!--画像即時にプレビューjavascript-->
        <script type="text/javascript">
        $(function() {
         function readURL(input) {
         if (input.files && input.files[0]) {
         var reader = new FileReader();
         reader.onload = function (e) {
           $(input).parents('.plan-image').find('#img2_prev').attr('src', e.target.result);
         }
         reader.readAsDataURL(input.files[0]);
         }
         }
         $(document).on('change', '.schedule2_img', function(){
         readURL(this);
         });
        });
        </script>
        
    </div>
    
    <div class="plan-image row row-0 mt-1">

          <div class="col-6 fileup pr-1">
          <% if f.object.image3? %>
            <%= image_tag f.object.image3.url, id: :img3_prev %>
            <p><%= f.check_box :remove_image3 %>画像を削除する</p>
          <% else %>
            <%= image_tag "default_plan3.jpg", id: :img3_prev %>
          <% end %>
            <label>
              <div class="xsmall">
              ファイルを選択
              </div>
              <div class="xsmall upload"><%= f.file_field :image3, class: :schedule3_img %></div>
            </label>
          </div>

        
        <!--画像即時にプレビューjavascript-->
        <script type="text/javascript">
        $(function() {
         function readURL(input) {
         if (input.files && input.files[0]) {
         var reader = new FileReader();
         reader.onload = function (e) {
           $(input).parents('.plan-image').find('#img3_prev').attr('src', e.target.result);
         }
         reader.readAsDataURL(input.files[0]);
         }
         }
         $(document).on('change', '.schedule3_img', function(){
         readURL(this);
         });
        });
        </script>
        
        
        <div class="col-6 fileup pl-1">
          <% if f.object.image4? %>
            <%= image_tag f.object.image4.url, id: :img4_prev %>
            <p><%= f.check_box :remove_image4 %>画像を削除する</p>
          <% else %>
            <%= image_tag "default_plan4.jpg", id: :img4_prev %>
          <% end %>
            <label>
              <div class="xsmall">
              ファイルを選択
              </div>
              <div class="xsmall upload"><%= f.file_field :image4, class: :schedule4_img %></div>
            </label>
        </div>
        
        <!--画像即時にプレビューjavascript-->
        <script type="text/javascript">
        $(function() {
         function readURL(input) {
         if (input.files && input.files[0]) {
         var reader = new FileReader();
         reader.onload = function (e) {
           $(input).parents('.plan-image').find('#img4_prev').attr('src', e.target.result);
         }
         reader.readAsDataURL(input.files[0]);
         }
         }
         $(document).on('change', '.schedule4_img', function(){
         readURL(this);
         });
        });
        </script>
        
    </div>
    
    
      <div class="xxsmall">※image1から順番に、5MB以下の JPG・PNG・GIFファイルを選択してください</div>
      <div class="xxsmall">※第三者の著作権、プライバシー権その他の権利を侵害しないよう留意してください</div>


      <div class="form-group mt-2">
        
        <%= f.label :content, 'スケジュールの説明' %>
        <%= f.text_area :content, :size => "80x5", placeholder: 'スケジュールの説明を追加 (1000文字以内)', class: 'form-control border-warning'  %>
        
        <div class="mt-2">
          <%= f.text_field :spot_name, placeholder: 'スポット名や店名などを入力', class: 'form-control border-warning' %>
        </div>
        
        <div class="mt-2">
          <%= f.text_field :address, placeholder: '住所を入力', class: 'form-control border-warning' %>
        </div>
        
        <div class="mt-2">
          <%= f.text_field :access, placeholder: 'アクセス 1：最寄り駅からの時間や道順などを入力', class: 'form-control border-warning'  %>
        </div>
        <div class="mt-2">
          <%= f.text_field :access2, placeholder: 'アクセス 2：最寄り駅からの時間や道順などを入力', class: 'form-control border-warning'  %>
        </div>
        
        

          <%= f.text_field :business_hour, placeholder: '営業時間', class: 'form-control border-warning mt-2' %>

          <%= f.text_field :regular_holiday, placeholder: '定休日', class: 'form-control border-warning mt-2' %>

          <%= f.text_field :tel, placeholder: '電話番号', class: 'form-control border-warning mt-2' %>

          <%= f.text_field :parking, placeholder: '駐車場 (有・無)', class: 'form-control border-warning mt-2' %>

          <%= f.text_field :budget, placeholder: '価格・予算', class: 'form-control border-warning mt-2' %>

          <%= f.url_field :link_url, placeholder: 'リンク(URL)', class: 'form-control border-warning mt-2' %>
          
          <div class="mt-2">
            <%= f.label :comment, 'コメント' %>
            <%= f.text_area :comment, :size => "80x2",  class: 'form-control border-warning'  %>
          </div>
          
      </div>
      
      
      <div class="text-right">
        <div class="schedule_links mt-3 btn  btn-sm btn-danger">
          <%= link_to_remove_association "× スケジュールを削除", f %>
        </div>
      </div>
      <hr>

      
    </div>      
