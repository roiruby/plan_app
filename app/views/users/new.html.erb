    <div class="container">
      <% breadcrumb :signup %>
      <%= render "layouts/breadcrumbs" %>
      <div class="row">
        
        <div class="offset-md-3 col-md-6 mt-5">
          <p class="bold h5 mb-1 ml-1 text-center">Pladuce 新規会員登録</p>
          <p class="text-center small">Pladuceに会員登録いただくと、プランナーになり</br>
            投稿やプランナーのフォローを行う事ができます。</br>
            登録はたったの2ステップ、1分でOK!!</p>

          <div class="bg-gray mb-5">
          <%= form_with(model: @user, local: true) do |f| %>
            <%= render 'layouts/error_messages', model: f.object %>

            <div class="form-group">
              <%= f.label :name, 'ユーザーネーム' %>
              <%= f.text_field :name, class: 'form-control' %>
            </div>

            <div class="form-group">
              <%= f.label :email, 'メールアドレス' %>
              <%= f.email_field :email, class: 'form-control' %>
            </div>

            <div class="form-group">
              <%= f.label :password, 'パスワード' %>
              <%= f.password_field :password, class: 'form-control' %>
            </div>
            
            <div class="a">
              <p class="mt-3 xsmall text-center"><%= link_to '利用規約', signup_path %>に同意の上ご登録ください</p>
            </div>
            
            <div class="text-center mt-3">
              <%= f.submit '登録する', class: 'btn btn-info bold' %>
            
          <% end %>
          
            <div class="a mt-4">
              <p class="mt-3 xsmall">すでにPladuceに登録されている方は<%= link_to 'こちら', login_path %></p>
            </div>
          
          </div>
          </div>
        </div>
      </div>
    </div>