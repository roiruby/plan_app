  <div class="container">
    
    <div class="mt-5 h3 bold">
      退会する
    </div>
    
    <div>
      <div class="mt-3 quitbg">
        Pladuceを退会するにあたって
      </div>
      <div class="medium mt-2">
        ・退会処理をすると、会員サービスがご利用できなくなりますのでご注意ください。<br>
        ・退会処理後にアカウントを元に戻すことはできませんのでお気をつけください。
      </div>
    </div>


    <div class="mt-5">
    <%= form_with(url: quit_path, model: @quit, local: true) do |f| %>

      <div class="form-group">
        <%= f.hidden_field :user_id, :value => @user.id %>
      </div>

      <div class="form-group">
         <%= f.label :content, 'Pladuceを退会する理由をお聞かせください（任意）' %>
         <%= f.text_area :content, size: '10x10', class: 'form-control' %>
      </div>
       
      <div class="text-center">
       <%= link_to "戻る", root_path, class: "btn btn-info" %>

        <%= f.submit '退会する', class: "btn btn-secondary", data: { confirm: '本当に退会しますか？' }  %>
      </div>

    <% end %>
    </div>

  </div>